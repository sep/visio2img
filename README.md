A command line tool to export all the pages of a Visio drawing to PNG.

## Command Line Usage ##

`visio2img path_to_drawing.vsd`


Accepts 1 command line argument, the file to export.

Exports each page as a PNG into the current working directory.


## Future ##

* specify output format
* specify output directory
* provide usage help to user
